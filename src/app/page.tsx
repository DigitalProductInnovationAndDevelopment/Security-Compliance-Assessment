export default async function Home() {
  // WIP: This is just a placeholder to show how the logic of the Home page can be implemented
  // const {data, isLoading, error} = api.query.projects()

  // if (isLoading) return <div>Loading...</div>

  // if (error) return <div>Error: {error.message}</div>

  // if (!data.length) {
  //   redirect('/refa')
  // } else {
  //   redirect('/projects')
  // }

  return (
    <div className="flex min-h-screen w-full flex-col px-6">
      Home
    </div>
  );
}
